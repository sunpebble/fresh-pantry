/// 圆角 token。
class AppRadius {
  AppRadius._();

  static const double xs = 4; // 细分隔 / 薄进度条 cap
  static const double sm = 8; // 小圆角(badge / chip 内嵌)
  static const double md = 12; // 默认 surface(SnackBar 等)
  static const double lg = 16; // 卡片
  static const double xl = 20; // FK 主卡 / dialog
  static const double xxl = 24; // 大卡片
  static const double hero = 28; // FK Hero block 底圆角
  static const double chip = 14; // FK search field / chip 矩形
  static const double pill = 999; // pill 形(stadium)
}
